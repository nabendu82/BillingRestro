# import * from './generated/prisma.graphql'

type Mutation {
    createItem(title: String, image: String, price: Int): Item!
    updateItem(id: ID!, title: String, price: Int): Item!
    deleteItem(id: ID!): Item
}

type Query {
    items: [Item]!
    item(where: ItemWhereUniqueInput!): Item
}