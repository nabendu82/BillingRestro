export interface CacheData {
    serviceClusterCache: CacheEntry[];
}
export interface CacheEntry {
    service: string;
    stage: string;
    cluster: string;
}
export declare class ClusterCache {
    cachePath: string;
    cache: CacheEntry[];
    constructor(cachePath: string);
    load(): void;
    addCacheEntry(entry: CacheEntry): void;
    save(): void;
    getEntriesByService(service: string): CacheEntry[];
}
