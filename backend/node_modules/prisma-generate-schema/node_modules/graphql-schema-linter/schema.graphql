# Query root
type QueryRoot {
  # Node
  node(id: Int!): Node
}

type user {
  fullName: String

  # test
  firstName: String @deprecated

  # test
  lastName: String @deprecated(reason: "Use fullName instead.")
}

# Node
interface Node {
  # Id
  id: ID!
}

schema {
  query: QueryRoot
}

extend type user {
  b: String
}

enum Stage {
  b
  Z
  A
}
