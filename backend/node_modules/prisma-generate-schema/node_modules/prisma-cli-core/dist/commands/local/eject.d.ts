import { Command } from 'prisma-cli-engine';
export default class Eject extends Command {
    static topic: string;
    static command: string;
    static description: string;
    static hidden: boolean;
    run(): Promise<void>;
    private getWindowsBat();
    private printBat(env);
}
