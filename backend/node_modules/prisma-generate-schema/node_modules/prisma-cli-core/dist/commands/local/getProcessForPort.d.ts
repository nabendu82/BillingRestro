export interface ProcessInfo {
    command?: string;
    processId: string;
}
export declare function getProcessForPort(port: any): ProcessInfo | null;
export declare const printProcess: ({command, processId}: ProcessInfo) => any;
