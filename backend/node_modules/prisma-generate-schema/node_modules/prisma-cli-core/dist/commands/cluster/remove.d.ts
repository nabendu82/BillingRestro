import { Command } from 'prisma-cli-engine';
export default class ClusterRemove extends Command {
    static topic: string;
    static command: string;
    static description: string;
    static group: string;
    run(): Promise<void>;
    getCluster(): Promise<string>;
    private askForConfirmation(cluster);
}
