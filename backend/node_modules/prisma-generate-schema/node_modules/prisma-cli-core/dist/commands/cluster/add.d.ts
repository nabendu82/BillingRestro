import { Command } from 'prisma-cli-engine';
export default class ClusterAdd extends Command {
    static topic: string;
    static command: string;
    static description: string;
    static group: string;
    run(): Promise<void>;
    private nameSelector();
    private endpointSelector();
    private clusterSecretSelector();
}
