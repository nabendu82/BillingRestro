import { Command, Flags } from 'prisma-cli-engine';
export default class Init extends Command {
    static topic: string;
    static description: string;
    static args: {
        name: string;
        description: string;
    }[];
    static flags: Flags;
    run(): Promise<void>;
    prompt(): Promise<'create' | 'minimal'>;
    initMinimal(): void;
    graphqlCreate(boilerplate?: string): Promise<void>;
}
