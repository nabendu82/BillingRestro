import { AuxillaryInterfaceGenerator } from '../generator';
import { GraphQLInterfaceType } from "graphql/type";
export default class NodeGenerator extends AuxillaryInterfaceGenerator {
    getTypeName(input: null, args: {}): string;
    protected generateInternal(input: null, args: {}): GraphQLInterfaceType;
}
