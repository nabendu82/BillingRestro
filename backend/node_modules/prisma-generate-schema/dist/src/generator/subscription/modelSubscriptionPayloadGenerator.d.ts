import { GraphQLFieldConfigMap } from 'graphql/type';
import { IGQLType } from '../../datamodel/model';
import { ModelObjectTypeGenerator } from '../generator';
export default class ModelSubscriptionPayloadGenerator extends ModelObjectTypeGenerator {
    getTypeName(input: IGQLType, args: {}): string;
    protected generateFields(input: IGQLType, args: {}): GraphQLFieldConfigMap<any, any>;
    private hasScalarField;
}
