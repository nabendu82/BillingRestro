import { RootGenerator } from "../generator";
import { IGQLType } from "../../datamodel/model";
import { GraphQLObjectType } from "graphql/type";
export default class MutationGenerator extends RootGenerator {
    getTypeName(input: IGQLType[], args: {}): string;
    protected generateInternal(input: IGQLType[], args: {}): GraphQLObjectType;
    private generateCreateField;
    private generateUpdateField;
    private generateUpdateManyField;
    private generateUpsertField;
    private generateDeleteField;
    private generateDeleteManyField;
}
