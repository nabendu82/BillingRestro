import { Command, Flags } from 'prisma-cli-engine';
export default class GenereateCommand extends Command {
    static topic: string;
    static description: string;
    static flags: Flags;
    run(): Promise<void>;
    generateSchema(output: string, schemaString: string): Promise<void>;
    generateTypescript(output: string, schemaString: string): Promise<void>;
    generateJavascript(output: string, schemaString: string): Promise<void>;
    generateGo(output: string, schemaString: string): Promise<void>;
    generateFlow(output: string, schemaString: string): Promise<void>;
    replaceEnv(str: any): string;
}
